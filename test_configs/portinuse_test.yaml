listen_address: "127.0.0.1:12345"
notification_url: ""
authenticated_hosts:
  authorized_host: "12345678"
host_max_seconds: 2
template_title: "{{ .ServiceName }} is {{ if .IsActive }}active{{ else }}inactive{{ end }}"
services:
  bash_echo:
    type: bash_script
    bash_script:
      code: "echo 'hello'"
      expected_output_regexp: "hello"
